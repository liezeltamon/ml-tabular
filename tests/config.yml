general:
  random_state: 290
  eval_metric: "auc"
  objective_type: "binary"
  test: true
  num_cores: 1

optimise:
  opt_direction: "maximize"
  opt_n_trials: 50

cross_val:
  cv_num_boost_round: 100

plot:
  plot_n_trials: null